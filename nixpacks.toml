# Nixpacks configuration for Railway PHP deployment
[phases.setup]
nixPkgs = ["php83", "php83Extensions.mysqli", "php83Extensions.pdo_mysql", "php83Extensions.curl", "php83Extensions.mbstring"]

[phases.install]
cmds = []

[start]
cmd = "php -S 0.0.0.0:${PORT:-8080} -t ."
